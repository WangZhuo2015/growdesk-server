# `@growdesk/adapters`

This package contains infrastructure adapter boundaries and small, deterministic
runtime helpers. Provider clients are added by their owning feature task; this
baseline does not claim an AI, Redis, S3, or notification implementation.
